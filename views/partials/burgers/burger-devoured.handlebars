<div class="mt-4">
	<h4 class="devoured-burger-name">{{burger_name}}</h4>

	<!--Throw away burger button-->
	<button class="delete-burger btn btn-primary" id="throw-away" data-id="{{this.id}}"><i class="far fa-trash-alt"></i> Throw Away</button>
</div>